# Core API
fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# Database
sqlalchemy>=2.0.28

# Validation
pydantic>=2.6.4
pydantic-settings>=2.2.1

# Data Science — use latest versions with Python 3.14 wheels
pandas>=2.2.2
numpy>=2.0.0
scikit-learn>=1.5.0

# Auth — use bcrypt directly (passlib 1.7.4 is incompatible with bcrypt 5.x)
pyjwt>=2.8.0
bcrypt>=4.1.3

# HTTP
httpx>=0.27.0
